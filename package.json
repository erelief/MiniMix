{
  "name": "minimix",
  "private": true,
  "version": "0.6.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-third-party-licenses.mjs && node scripts/check-i18n.js && vite build",
    "check:i18n": "node scripts/check-i18n.js",
    "gen:licenses": "node scripts/generate-third-party-licenses.mjs",
    "prepare": "node scripts/generate-third-party-licenses.mjs",
    "preview": "vite preview",
    "sync-version": "node scripts/sync-version.js",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "node scripts/sync-version.js && node scripts/generate-third-party-licenses.mjs && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "vite": "^6.3.0"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-fs": "^2.4.5",
    "@tauri-apps/plugin-process": "^2",
    "@tauri-apps/plugin-shell": "^2.3.5",
    "@tauri-apps/plugin-updater": "^2",
    "lucide": "^1.14.0"
  }
}
